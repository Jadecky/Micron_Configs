[gcode_macro WLED_ON]
description: Turn WLED strip on using optional preset and resets led colors
gcode:
  {% set strip = params.STRIP|default("chamber")|string %}
  {% set preset = params.PRESET|default(4)|int %}

  {action_call_remote_method("set_wled_state",
                             strip=strip,
                             state=True,
                             preset=preset)}


[gcode_macro LED_HEATING]
gcode:
  WLED_ON STRIP=chamber PRESET=4
  WLED_ON STRIP=deskLight PRESET=2

[gcode_macro LED_PRINT_END]
gcode:
  WLED_ON STRIP=deskLight PRESET=3

[gcode_macro LED_PRINTING]
gcode:
  WLED_ON STRIP=deskLight PRESET=4

[gcode_macro LED_STANDBY]
gcode:
  WLED_ON STRIP=deskLight PRESET=1